import variables from './variables.module.scss';

export const breakpoints = {
  xs: `${variables.xs}px`,
  sm: `${variables.sm}px`,
  sd: `${variables.sd}px`,
  md: `${variables.md}px`,
  lg: `${variables.lg}px`,
  xl: `${variables.xl}px`
};
